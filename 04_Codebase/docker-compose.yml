services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: xbot_ev
      POSTGRES_USER: xbot
      POSTGRES_PASSWORD: xbot
    ports:
      - "5432:5432"
    volumes:
      - xbot_ev_pgdata:/var/lib/postgresql/data

volumes:
  xbot_ev_pgdata:
